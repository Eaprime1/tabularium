# Instincts generated from https://github.com/Eaprime1/tabularium
# Generated: 2026-07-08T10:20:02.199Z
# Version: 2.0
# NOTE: This file supplements (does not replace) any existing curated instincts.
# High-confidence manually curated instincts should be preserved alongside these.

instincts:
  - id: tabularium-naming-files
    trigger: "when creating a new file"
    confidence: 0.8
    domain: code-style
    source: repo-analysis
    source_repo: https://github.com/Eaprime1/tabularium
    title: "Tabularium Naming Files"
    action: "Use snake_case naming convention"
    evidence:
      - "Analyzed file naming patterns in repository"
      - "Dominant pattern: snake_case"

  - id: tabularium-import-absolute
    trigger: "when importing modules"
    confidence: 0.9
    domain: code-style
    source: manual-verification
    source_repo: https://github.com/Eaprime1/tabularium
    title: "Tabularium Import Style"
    action: "Use absolute, standard-library-only imports (e.g. `import json`, `from pathlib import Path`). There is no package structure and no relative imports anywhere in pipeline/ or tools/."
    evidence:
      - "Verified against every pipeline/*.py and tools/*.py file — zero relative imports found"

  - id: tabularium-export-style
    trigger: "when exporting from a module"
    confidence: 0.9
    domain: code-style
    source: manual-verification
    source_repo: https://github.com/Eaprime1/tabularium
    title: "Tabularium Export Style"
    action: "No module-level exports exist. Each script is a standalone CLI entry point (if __name__ == '__main__': main()); do not add __all__."
    evidence:
      - "Verified: no __all__ and no cross-script imports anywhere in the repo"

  - id: tabularium-no-test-suite
    trigger: "when writing tests"
    confidence: 0.9
    domain: testing
    source: manual-verification
    source_repo: https://github.com/Eaprime1/tabularium
    title: "Tabularium Has No Test Suite"
    action: "Do not assume a tests/ directory, pytest, or unittest. Correctness is checked with python3 -m py_compile plus the pipeline's own before/after snapshot diff — if adding real tests, discuss the framework choice first."
    evidence:
      - "Verified: no tests/, __tests__/, *.test.*, pytest, or unittest usage found anywhere in the repo"

  - id: tabularium-instinct-function-naming
    trigger: "When defining a new function"
    confidence: 0.9
    domain: code-style
    source: manual-verification
    source_repo: Eaprime1/tabularium
    title: "Tabularium Instinct Function Naming"
    action: "Name the function using snake_case (e.g. do_copies, do_dedup, unique_path)"
    evidence:
      - "Verified against every pipeline/*.py and tools/*.py file — camelCase never used"

  - id: tabularium-instinct-constant-naming
    trigger: "When defining a module-level constant"
    confidence: 0.9
    domain: code-style
    source: manual-verification
    source_repo: Eaprime1/tabularium
    title: "Tabularium Instinct Constant Naming"
    action: "Name the constant using SCREAMING_SNAKE_CASE (e.g. LIBRARY_BASE, ADAPTERS, GROUPS)"
    evidence:
      - "Verified against pipeline/crispr_run.py, crispr_rename.py, crispr_consolidate.py"

