interface:
  display_name: "Tabularium"
  short_description: "Repo-specific patterns and workflows for tabularium"
  default_prompt: "Use the tabularium repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true